<h3 class='text-dark'>Friend requests</h3>
<% @friendship_requests.each do |request| %>
  <%= link_to request.user.name, users_path(request.user) %>
  <%= link_to 'Confirm', friendships_path(request.id), method: :update %> 
  <%= link_to 'Delete', friendships_path(request.id), method: :destroy %><br>
<% end %>
<br>
Friends: <br>
<% @friends.each do |friend| %>
  <%= link_to friend.name, users_path(friend) %> <br>
<% end %>
</ul>