<h3 class='text-dark'>Friend requests</h3>
<% @friendship_requests.each do |request| %>
  <%= link_to request.user.name, users_path(request.user) %>
  <%= link_to 'Confirm', friendship_path(request.user.id), method: :put, class: "btn btn-info" %> 
  <%= link_to 'Delete', friendship_path(request.user.id), method: :delete, class: "btn btn-warning" %><br><br>
<% end %>
<br>
Friends: <br>
<% @friends.each do |friend| %>
  <%= link_to friend.name, users_path(friend) %> <br>
  <%= link_to 'Unfriend', friendship_path(request), method: :unfriend, class: "btn btn-warning" %><br><br>
<% end %>
</ul>